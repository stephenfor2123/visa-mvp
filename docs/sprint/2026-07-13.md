# 2026-07-13

## 状态

`deployed` — 2026-07-13 已推 GitHub + 阿里云 API；**生产 PAYMENT_CHANNEL=mock**（Stripe 未开）

## 建议 commit message

```
feat(admin): platform pricing promo config and pending web updates
```

（若当天还包含登录/联系页等，可改成更宽的主题，例如 `feat: pricing admin, contact i18n, and deploy fixes`）

---

## 功能清单

### A. 价格与营销（本次对话新增，**未上线**）

- [ ] 后台 **价格营销** 页 `/admin/pricing`（划线价、促销价、有效期）
- [ ] 表 `platform_pricing` + 迁移 `0024_platform_pricing`
- [ ] 公开 API `GET /api/v2/pricing/current`
- [ ] 前台 `PricingSection` 划线价 + 促销价（读 API）
- [ ] 权限 `pricing.manage`；菜单对 `settings` 或 `pricing.manage` 可见
- [ ] `require_perm_any` 支持 settings 进定价 API

### B. 工作区其他已改未提交（是否并入由你决定）

- [ ] Admin 用户页 / 登录页小改
- [ ] Contact、Login、Register、PaymentCheckout、i18n 多语言
- [x] `backend/app/api/v2/payment.py`、`payment_provider`、Stripe 测试（见 **D. 支付**）
- [ ] iOS l10n、vercel/vite 配置
- [ ] `README.md`、`docs/PRE_LAUNCH_OWNER_TODO.md` 等文档

### D. 支付通道（Session: Stripe Phase A + 幂等 + 退款）

- [x] Stripe Test 密钥接入（`PAYMENT_CHANNEL=stripe`，`backend/.env` / `frontend/web/.env`）
- [x] `POST /api/v2/payment/stripe-webhook` 验签 + `webhook_events` 幂等去重
- [x] `GET /api/v2/payment/config` 返回 channel + publishable key
- [x] `POST /api/v2/payment/create` Stripe PaymentIntent + Mock 双通道
- [x] `POST /api/v2/payment/{order_no}/refund` 退款（Mock + Stripe `Refund.create`）
- [x] **幂等 / 防重复支付**
  - [x] `pending` 且金额/币种/未过期 → 重复 `create` 返回**同一 `trade_no`**（不新发流水）
  - [x] `paid` → 重复 `create` 返回 **409 / 4013**（禁止二次扣款）
  - [x] `notify` / webhook：`paid` / `refunded` 重放安全；**`trade_no` 不匹配**拒绝入账
  - [x]  supersede 旧 `pending`/`closed` 时归档至 `orders.extra.payment_history[]`
  - [x] Stripe supersede 时 best-effort `PaymentIntent.cancel` 旧 intent
  - [x] 退款二次调用幂等（同一 `refund_trade_no`）
- [x] 本地代理绕过：`api.stripe.com` 加入 `NO_PROXY`（防 HTTP_PROXY 403）
- [x] 集成测试 `tests/integration/test_payment.py` **17** 项 + `test_stripe_phase_a_webhook.py` **3** 项全绿
- [ ] 生产 Live 密钥 + Dashboard Webhook（Owner：`https://api.htexvisa.com/api/v2/payment/stripe-webhook`）

### C. 已在仓库、本次不必重复 commit

- 订单状态机 v2、退款副轨、Admin 订单关注/资金流/日志（commit `bac6b2b` 等）
- 迁移 `0023_order_state_v2`

---

## 数据库 / 迁移

```bash
cd backend && alembic upgrade head   # 含 0024_platform_pricing
```

**0024 默认种子：**

| 字段 | 值 |
|------|-----|
| 划线价 | $99.90 |
| 促销价 | $19.90 |
| 有效期 | 2026-07-15 00:00 ~ 2026-08-15 23:59 UTC |

---

## 部署后验证

- [ ] 后台侧边栏出现 **价格营销**（有 `settings` 或 `pricing.manage`）
- [ ] 打开 `/admin/pricing` 可改价并保存
- [ ] `GET /api/v2/pricing/current` 返回 `is_promo` / `display_price_usd`
- [ ] 首页/定价页平台服务费显示划线价（促销期内）
- [ ] 退出后台重新登录（刷新 permissions）
- [ ] Admin 用户列表不再 NETWORK（若仍失败，查 API 域名/CORS）
- [ ] `GET /api/v2/payment/config` → `channel: stripe` + `pk_test_` / `pk_live_`
- [ ] 测试卡 `4242…` 支付后订单 `paid`；重复点支付不生成第二笔扣款（同 `trade_no` 或 4013）
- [ ] `POST /payment/{order_no}/refund` 退款后 `GET /payment/{order_no}` → `refunded`
- [ ] Stripe Dashboard Webhook 投递 `payment_intent.succeeded` 幂等（重放不重复改状态）

---

## 变更文件（摘要）

### 价格营销 — 新增 (new)

```
backend/alembic/versions/0024_platform_pricing.py
backend/app/models/platform_pricing.py
backend/app/schemas/pricing.py
backend/app/services/pricing_service.py
backend/app/api/v2/pricing.py
frontend/web/src/views/admin/AdminPricing.vue
frontend/web/src/api/pricing.js
frontend/web/src/composables/usePlatformPricing.js
```

### 支付 Stripe Phase A — 修改

```
backend/app/api/v2/payment.py          # create/close/refund/stripe-webhook/config
backend/app/services/payment_provider.py  # Mock+Stripe 幂等、退款、payment_history
backend/app/schemas/payment.py         # RefundPayment*、Query 含 refunded 字段
backend/app/core/errors.py             # PAYMENT_SIGNATURE_INVALID 4015
backend/tests/integration/test_payment.py  # 含幂等/退款用例
backend/tests/test_stripe_phase_a_webhook.py
backend/tests/conftest.py              # PAYMENT_CHANNEL=mock、RESEND 空（测试隔离）
docs/STRIPE_PHASE_A.md
```

### 价格营销 — 修改

```
backend/app/api/v2/__init__.py
backend/app/api/v2/admin.py
backend/app/core/permissions.py
backend/app/middleware/admin_auth.py
backend/app/models/__init__.py
frontend/web/src/api/admin.js
frontend/web/src/components/PricingSection.vue
frontend/web/src/router/admin.js
frontend/web/src/views/admin/AdminLayout.vue
frontend/shared/i18n/zh-CN.json
frontend/shared/i18n/en.json
```

### 工作区其他（可选并入同日 commit）

```
# 见 git status — 约 47 个已跟踪文件有改动 + 大量未跟踪截图
git status
```

---

## 不纳入本次 commit

- 根目录 `pricing_*.png`、`contact_*.png`、`mega_*.png` 等截图
- `_plane-loader-preview.html`、`_truck-loader-preview.html`
- `browser-extension/htex-ds160-extension-v0.2.*.zip`（除非刻意发版 extension）
- `.env`、任何含密钥的文件

---

## 备注

- 生产 **htexvisa.com** 当前无「价格营销」菜单：需 **build + deploy + migrate** 后才有。
- Commit: `317b56a`
- 发版前更新本文件状态为 `ready`，commit 后改 `INDEX.md` 填 hash。
